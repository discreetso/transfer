as we know p.getArea does not make any for that we want to write in our code specifically that it does not make any sense because it returns 0 and it is not a valid value. One way, we can throw an exception. In python, we use this way when we have to override any function which does not return any valid value we throw exception raise notimplemented. In java, we made that function abstract. In grammer, abstract means having no physical appearance, beauty or whiteness. In java programming, abstrat function are that function which does not have any body because it does not have any return value. We only want this function in the parent class to override it in the child class. Why write this abstract function, so that child calss can define it. 
Now there's another problem that we have made such a class which have an abstract function which cannot be created because it does not have any body, it does not make any sense. if one such thing comes in a class, now you can't even make an instance of this class. Because if instance created, this function is also necessary to be created but it has not any body and it is an abstract function, so now we have two ways, one is make this function's body or also make this class abstract. Now here, it is necessary to abstract this class. There's a portion in the body which can't be created so the whole body can't be created. 
Now there's a principle of abstract class that we can't make its object. Now if you try to make this class body or objec tit gives you an error that "Polygon is abstract, can't be instantiated". Now the whole polygon class is finished. then why created?
The reason of making this class, when we say ""
